name: dbt CI

on:
  pull_request:
    paths:
      - "dbt/**"

env:
  DBT_PROJECT_DIR: dbt
  DBT_PROFILES_DIR: dbt

jobs:
  dbt-check:
    name: dbt compile & test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dbt
        run: pip install dbt-core dbt-bigquery

      - name: Install dbt packages
        working-directory: ${{ env.DBT_PROJECT_DIR }}
        run: dbt deps

      # Compile check (no credentials needed)
      - name: dbt parse
        working-directory: ${{ env.DBT_PROJECT_DIR }}
        run: dbt parse

      # Full build against BigQuery (needs credentials in Github secrets)
      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Write CI profiles.yml
        working-directory: ${{ env.DBT_PROJECT_DIR }}
        run: |
          cat > profiles.yml <<'YAML'
          nhs_ed_pipeline:
            target: ci
            outputs:
              ci:
                type: bigquery
                method: oauth
                project: guys-nhs
                dataset: ci_pr_${{ github.event.pull_request.number }}
                location: europe-west2
                threads: 4
          YAML

      - name: dbt build (run + test)
        working-directory: ${{ env.DBT_PROJECT_DIR }}
        run: dbt build

      - name: Tear down CI dataset
        if: always()
        run: |
          bq rm -r -f "guys-nhs:ci_pr_${{ github.event.pull_request.number }}" || true
